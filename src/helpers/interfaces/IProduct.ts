export interface IProduct {
    id: string;
    name: string;
    description: string;
    productImage: string;
    productImageFilename: string;
    userId: string;
    categoryId: string;
    unitId: string;
}